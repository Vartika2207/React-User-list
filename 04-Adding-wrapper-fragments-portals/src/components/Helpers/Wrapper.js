const Wrapper = (props) => {
    return (props.children);/*children prop holds all the content passed between the opening and closing tag
                              off your custom component.*/
};

export default Wrapper;